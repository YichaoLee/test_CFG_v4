#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;
double t;

		if(x * y +  atan(z) *  sin(w*t) > x/y + z +  tan(w+t)) {
			printf("Solved 56");
		}
return 0;	
}
