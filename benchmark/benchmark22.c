#include <math.h>
#include <stdio.h>
int main(){
double x;
double y;
double z;
double w;
double v; 
double t;
double q;
		if(( sin( cos(x*y)) <  cos( sin(y*z))) && sin(w*2.0 -y)/( cos(t*2.0+q)+1.0) == ( cos(z*2.0+x)/( sin(w*2.0+v)+1.0))) {
			printf("Solved 22");
		}
return 0;	
}
