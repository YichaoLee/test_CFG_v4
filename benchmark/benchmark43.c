#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;



	  if( pow(x,y) >  pow(y,x) &&  exp(y) >  exp(x) && y <  pow(x,2)) {
		  printf("Solved 43");
	  }
return 0;	
}
