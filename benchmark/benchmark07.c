#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;

    if ( sin(2 * x - y) / ( cos(2 * y + y) + 1) ==  cos(2 * z + x)
        / ( sin(2 * w + y) - 1)) {
      printf("Solved 07");
    }
return 0;	
}
