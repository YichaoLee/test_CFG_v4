#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;

	  if( sin(2*x - y)/( cos(2*y + x) + 1) ==  cos(2*z + x)/( sin(2*w + y) - 1) &&
		  sin(x*y*z*w) > 0 &&  cos(x*y*z*w) < 0) {
      printf("Solved 23");
    }
return 0;	
}
