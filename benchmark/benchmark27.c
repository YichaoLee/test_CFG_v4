#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;



	if( sin(x* cos(y* sin(z))) >  cos(x* sin(y* cos(z))) &&  sin( cos(x*y)) <  cos( sin(x*y))) {
      printf("Solved 27");
    }
return 0;	
}
