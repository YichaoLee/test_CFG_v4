#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;



		if( sqrt( exp(x+y)) <  pow(z,x) && x > 0 && y > 1 && z > 1 && y <= x + 2) {
			printf("Solved 45");
		}
return 0;	
}
