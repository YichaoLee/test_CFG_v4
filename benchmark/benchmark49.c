#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;


	  if( pow(x,2) + 3* sqrt(y) < x*y && x <  pow(y,2) && x + y < 50) {
		  printf("Solved 49");
	  }
return 0;	
}
