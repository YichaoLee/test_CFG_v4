#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;
double v; 

	  if( sin( cos(x*y)) <  cos( sin(x*z)) && ( sin(2*w - y)/( cos(2*y + v) + 1) ==  cos(2*z + x)/( sin(2*w + v) - 1))) {
		  printf("Solved 25");
    }
return 0;	
}
