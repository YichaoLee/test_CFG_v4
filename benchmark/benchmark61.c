#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;
double t;

		if(x + y > z / w &&  sqrt(x) > z / y && z*2 + w*3 + x*7 <  pow(y,6) && z + w > x + y && w < x/y) {
			printf("Solved 61");
		}
	return 0;
}
