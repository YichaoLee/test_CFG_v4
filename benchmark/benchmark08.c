#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;

    if ( cos(3 * x + 2 * y - z) *  sin(z + x + y) == cos(z * x * y)) {

      printf("Solved 08");
    }
	return 0;
}
