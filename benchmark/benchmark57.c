#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;
double t;

		if(x * y +  atan(z) *  sin(w*t) > x/y + z +  tan(w+t) &&  pow( log10(x), log10(y)) <=  pow( log10(z+w+t), tan(w*t))) {
			printf("Solved 57");
		}
return 0;	
}
