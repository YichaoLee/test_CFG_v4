#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;


    if ( sin(x) +  sin(y) +  sin(z) == 4 *  cos(x)
        *  cos(y) *  cos(z)) {
      printf("Solved 05");
    }
return 0;	
}
