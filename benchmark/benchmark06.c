#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;


    if ( cos(x) +  cos(y) +  cos(z) > 4 *  sin(x / 2)
        *  sin(y / 2) *  sin(z / 2)) {
      printf("Solved 06");
    }
return 0;	
}
