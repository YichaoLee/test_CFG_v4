#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;


	if( sin(y) *  asin(x) - 300 <  cos(y)* cos(z) -  atan(w)) {
      printf("Solved 14");
    }
return 0;	
}
