#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;
double v;

	  if( sin( sin(x*y)) < 0 &&  cos(2*x) > 0.25) {
		  printf("Solved 20");
	  }
return 0;	
}
