#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;


	  if( sqrt( exp(x + z)) <  pow(z,x) && x > 0 && y > 1 && z > 1 && y < 1 && y < x + 2 && w == x + 2) {
		  printf("Solved 46");
	  }
	return 0;
}
