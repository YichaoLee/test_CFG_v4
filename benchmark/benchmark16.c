#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;
double v;

		if( tan(w-x)* cos( sin(w/v)) -  atan(y + 20 + z) +  asin(y-15) <  sin(w * w) *  cos(x*w*v) -  tan( cos(x*w*x)) +  sin(w)){
      printf("Solved 16");
    }
return 0;	
}
