#include <math.h>
#include <stdio.h>

int main(){
double x;
double y;
double z;
double w;


		if( pow(x, tan(y)) + z < x *  atan(z) &&  sin(y) +  cos(y) +  tan(y) >= x - z &&  atan(x) +  atan(y) > y &&  log( pow(x, tan(y))) <  log(z) &&   sqrt(y+z) >  sqrt( pow(x,(x-y)))) {
			printf("Solved 55");
		}
	return 0;
}
