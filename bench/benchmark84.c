#include <math.h>
#include <stdio.h>

int main(){
double a;
double b;
double c;
double d;


		if((10 * (b - a * a)) == 0 && (1 - a) == 0 && (sqrt(90) * (d - c * c)) == 0 && (1 - c) == 0 && (sqrt(10) * (b + d - 2)) == 0 && (pow(10, -0.5) * (b - d)) == 0) {
			printf("Solved 84");
		}
		
	return 0;
}
